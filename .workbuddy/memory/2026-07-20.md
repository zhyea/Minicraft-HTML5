# 2026-07-20 工作日志

- 用户问"游戏能否存档"：全仓 grep 确认当前无任何 save/load/persist/localStorage 调用，原版 Minicraft（Ludum Dare 小品）本无存档，HTML5 移植也未补。无"继续游戏"菜单。
- 用户要"出一版存档方案"，调度 engineering-lead 产出 `docs/architecture/save-system-plan.md`（设计级，含 ADR-1~4）。
  - 关键决策：localStorage（GWT 原生 `com.google.gwt.storage.Storage` 同步 API）> IndexedDB/文件；JSON 容器 + 5层×2个 `byte[]`(tiles/data) 用 Base64；全量快照覆盖写；存 `schemaVersion` 做版本兼容。
  - 体积：tiles+data 约 160KB，整体 <0.6MB，远低于 5MB 上限。
  - 读档必须绕开 `Level(int,int,int,Level)` 构造体（其固调 LevelGen + 父层楼梯开挖 + level==1 时 add AirWizard），需新增 `Level.fromSave` 工厂纯灌入。
  - `Game` 新增 saveGame()/loadGame()/hasSave() + 关闭/定时自动存；TitleMenu 动态"继续游戏"。
  - 验证要点：gwt.xml 确仅 inherits User/Canvas/gwt-voices，未 inherits Storage（落地需加一行）。沙箱无 Java/GWT 编译器，方案未全量实现，需本地 `ant clean && ant build` + GWT 2.4 验证。
- 用户说"按方案执行"，调度 engineering-lead-2 实现存档 MVP 阶段1（代码落地，非纯文档）。主理人"信任但验证"抽查 PASS：
  - 新增 src/com/mojang/ld22/save/{SaveStore,SaveManager,EntityIO,ItemIO}.java
  - 修改 Minicraft.gwt.xml（加 Storage inherits）、Level（fromSave 工厂：私有无参构造 `new Level()` 不调 LevelGen/不开挖楼梯/不 add AirWizard，按 depth 确定性重算 dirtColor/monsterDensity）、Game（saveGame/loadGame/hasSave + WindowClosingHandler 关闭存 + tick 每 1800 tick≈30s 自动存；saveGame 内 `if (player==null||player.removed) return` 规避"读档即死"）、Player（seedStartingItems 开关）、TitleMenu（动态「继续游戏」前置，选中 loadGame）、Resource/ToolType/Furniture（getByName/createByName 反查工厂）、Mob/Zombie/Slime/ItemEntity（字段可见性放宽供跨包序列化）
  - Base64 用纯 Java 手写编解码（非 JSNI btoa/atob），因 gwt.xml user.agent 含 ie9 无 btoa/atob；工程成员用 Node 5000 组随机 round-trip 验证 0 失败
  - 中文无回退（「继续游戏」已落地）；未触碰 Screen/Font/render/resize/汉化
  - 沙箱无 Java/GWT 编译器，未真编译；仍需本地 `ant clean && ant build` + GWT 2.4 SDK 验证
- 用户嫌 Ant 老，问有无其他编译工具。确认 build.xml 实质仅两步：javac src/ → com.google.gwt.dev.Compiler -war out/gwt-js-war com.mojang.Minicraft（GWT 2.4.0 SDK 在 ../gwt-2.4.0）。用户选 Gradle，调度 engineering-lead 产出等价 build.gradle + settings.gradle（JavaExec 调 GWT 2.4 编译器，classpath 含 gwt-user/gwt-dev/validation-api/gwt-voices + src 目录；build 任务等价 Ant gwt-build；保留 build.xml 兜底）。主理人验证 PASS：两步逐点对应、未动 src/。注意 GWT 2.4 须 JDK 8 + Gradle 8.x，jar 来自本地 SDK 非 Maven Central。
- 用户问能否用 Vue 重建（GWT 已是 Java→JS 编译器，产物即 JS，问题在不可维护）。选先做可行性研究，engineering-lead 产出 docs/architecture/vue-rebuild-feasibility.md。质量门 PASS：grep 核实 9 个 `import com.google.gwt` 文件 + Sound.java(gwt-voices) = 10 个 GWT 耦合需重写，其余 73 个纯逻辑可 port。结论：条件可行，全量重写为 TS 逻辑 + Canvas 渲染 + Vue 菜单 overlay，约 60 人日量级；建议阶段0 垂直切片先验证端到端。当前未动工。
- 阶段0 垂直切片 ts-port 已建并验证绿（详见 2026-07-19 日志）。用户授权"启动阶段1 全逻辑 port"，主理人拆 6 冲刺(S1物品背包/S2合成/S3家具/S4 mob+Boss/S5菜单树/S6粒子+缺失Tile)，每冲刺带 tsc/vitest/vite build 三道门控 + 主理人独立复核。
  - **S1 物品与背包+资源注册表：DONE & 主理人独立复核绿**（tsc exit0 / vitest 5文件28测试 / vite build exit0）。engineering-lead-4 产出 src/game/item/{Item,ToolType,ResourceItem,ToolItem,FurnitureItem,PowerGloveItem}.ts + item/resource/{Resource,FoodResource,PlantableResource,registry}.ts + entity/Inventory.ts，加 17 个新测试。已知偏差：5 个 PlantableResource 占位 Tile(treeSapling/hole/wheat/farmland/cactusSapling) 为 null、Furniture 实体留 S3、Player 加 heal()/payStamina()。
  - **S2 合成系统：DONE & 主理人独立复核绿**（tsc exit0 / vitest 6文件37测试 / vite build exit0，63 modules）。engineering-lead-5 产出 src/game/crafting/{Recipe,ResourceRecipe,ToolRecipe,FurnitureRecipe,Crafting}.ts + game/__tests__/crafting.test.ts(9测试) + Game.ts 接 installRecipes()。配方 35 个逐字保真 Java（已逐条核对 LANTERN wood5/slime10/glass4 等；grep 行数差 35 vs 37 纯因 TS 每 addCost 占一行、Java 每条配方占一行，非真实差异）。偏差：Recipe 收 Inventory 直接(非Player)；craft()不扣成本（Java 同，需配 deductCost()）；FurnitureStub 占位(留S3)；名称汉化(灯笼/烤箱/…)。
  - **S3 家具系统：DONE & 主理人独立复核绿**（tsc exit0 / vitest 7文件43测试 / vite build exit0，71 modules）。engineering-lead-6 产出 entity/Furniture.ts(基类 extends Entity)+6 子类(Workbench/Anvil/Furnace/Oven/Chest/Lantern；Chest 含 Inventory、Lantern lightRadius=8)+furniture.test.ts(6测试)；FurnitureItem 改引用真实 Furniture 类，FurnitureRecipe.FurnitureType.newInst() 建真实子类，S2 FurnitureStub 已删(仅注释/test 提及)。偏差：Furnace/Oven 的 Java 源码本无逐 tick 熔炼逻辑(仅 use 开菜单)，熔炼为菜单驱动留 S5；use 经 onUse hook 占位(S5 接 Vue 菜单)。注：本轮误双派 engineering-lead-7(S3 重复代理)已 cancel，未影响磁盘结果。
  - **S4 剩余 mob+Boss + 胜利条件：DONE & 主理人独立复核绿**（tsc exit0 / vitest 8文件47测试 / vite build exit0，73 modules）。engineering-lead-6 产出 entity/{Slime,AirWizard,Spark}.ts + mob.test.ts(4测试)；改 Player.gameWon()(invuln=60*5→game.won())、Game.won()/hasWon/wonTimer(=180)、Level.ts(level===1 在(w*8,h*8)召唤 AirWizard)。胜利连线逐字保真 Java(AirWizard.java:153→player.gameWon()→Game.won()=180；Player.gameWon() invuln=60*5 见 Java:402；Slime.java:94 接触伤害用 lvl 非 lvl+1，核对 Java 确为 lvl，属保真非偏差)。**已知缺口**：Sky 层未在 startNewGame 自动生成——createSkyMap 多生成 infiniteFall，validate 重试致 ~47s 卡死（LevelGen 既有隐患，因从不建 Sky 层未触发）；胜利逻辑已接通但端到端暂不可达，留 S6 修 LevelGen.createSkyMap 后建 levels[4] 补端到端召唤测试。
  - **S5 完整菜单树(Vue)：DONE & 主理人独立复核绿**（tsc exit0 / vitest 9文件54测试 / vite build exit0，98 modules）。engineering-lead-9 产出 src/ui/{InventoryMenu,CraftingMenu,ContainerMenu,DeadMenu,WonMenu,HelpMenu,AboutMenu}.vue + ItemIcon.vue + itemIcon.ts + state.ts + __tests__/menus.test.ts(7测试)；改 App.vue(overlay 由 gameState.currentMenu 驱动)、Game.tick(160-163 hasWon→openWon / player.health<=0→openDead)、Furniture.onUse→openCrafting/openContainer、ToolItem.matches() 改按 type.name 比较(应对 Vue reactive 代理单例失真，属保真适配)；零回归(54 vs 47)。**偏差披露**：vite.config 加 emptyOutDir:false(沙箱禁删致 Vite emptyDir 清扫被拦的绕过，非逻辑改动)；并存 vite.config.mjs+.ts 双配置 + 一堆 vite.config.*.timestamp-*.mjs 临时文件(沙箱禁删无法清理)。jsdom 测试对 canvas.getContext 报 Not implemented 噪声(图标在测试环境不渲染)，不影响断言，真实浏览器正常。用户选「清理临时文件+删冗余.ts」但沙箱禁删(已 deny)，清理留待本地/放开删除权限后执行。
  - **S6 粒子+缺失 Tile+Sky 生成修复+打磨：DONE & 主理人独立复核绿**（tsc exit0 / vitest 11文件64测试 / vite build exit0，104 modules）。engineering-lead-11 产出 tile/{Sapling,Hole,Farm,Wheat}.ts + entity/particle/{Particle,SmashParticle,TextParticle}.ts + sprint6.test.ts(9测试)；改 Game.startNewGame(建全5层 Sky[4]/surface[3]/under[2..0] 带 parent 链)、Level.ts(level===1 在(w*8,h*8)召唤 AirWizard，Java 保真)、Tile.ts(+5 静态)、tile/registry + item/resource/registry(清 NULL_TILE 占位：acorn→treeSapling/dirt 源含 hole+water+lava/cactusFlower→cactusSapling/seeds→wheat on farmland，无 null tile 残留)、Mob/Player/Cactus 接粒子、engine/Rand.nextGaussian()。**关键诚实偏差**：根因非 createSkyMap 卡死——实测当前 TS createSkyMap 300 样本 cloud∈[4360,6213] 永久≥2000、createAndValidateSkyMap 第1次 attempt ~1ms 不挂；真因是 startNewGame 从不建 Sky 层(及 levels[1/0])，修复=照 Java resetGame 建全5层使 Sky 层 AirWizard 端到端可达，LevelGen 算法零改动。**其余偏差(均属切片范围外/一致策略)**：① WheatTile.harvest/CactusTile.hurt 掉落物 stub(未 port ItemEntity，与既有掉落 stub 策略一致) ② TextParticle.render 画彩色标记非 Font.draw 文字(无 Font 模块) ③ SmashParticle 省 Sound.monsterHurt(无音频) ④ StoneTile 故意跳过(Java 全源未引用，死代码)。**遗留 TODO(非阻塞，超出阶段1 六冲刺定义范围)**：vite.config 双配置+timestamp 临时文件清理(沙箱禁删，待本地/放开删权)；port ItemEntity 打通 Wheat/Cactus 掉落；port goldOre/gemOre 让深层渲染矿脉而非回退 rock；sky-scratch.test.ts 已成 noop 占位可删。
  - 注：SendMessage 在本会话不可用(agent 完成后非运行态)，续冲刺改以"重新派发独立自包含 agent"方式推进，质量不受影响(agent 直接读盘)。任务系统跨会话清空，本轮重建冲刺任务(#1~#5)。
- **TS 版存档功能(MVP 闭环)落地**：用户问"游戏的存档功能有做实现么"→ 澄清双线(GWT/Java 侧已实现未编译验证；ts-port 侧此前零存档)→ 用户拍板"在 TS 版加存档"→ 范围=MVP 闭环(定时30s+beforeunload 自动存 + 标题「继续游戏」+ schemaVersion + 异常兜底；死亡/通关不自动存)。engineering-lead-1 产出并主理人独立复核绿(tsc exit0 / vitest 72测试(原64+新增8 save.test) / vite build 109 modules exit0)。
  - 新增 ts-port/src/game/save/{SaveStore,SaveManager,EntityIO,ItemIO}.ts + __tests__/save.test.ts(8测试：全量往返/ hasSave状态 / Sky不重复Boss / 版本损坏容错 / 自动存档节拍 / 实体多样性)。
  - SaveStore：localStorage 键 `minicraft.save`；btoa/atob 做 Uint8Array↔Base64；localStorage 不可用时回退模块级内存 Map(共享后端，g1存g2读互通)；写盘全程 try/catch 静默。
  - SaveManager：toJson/fromJson，Player 单独持久化避免重复；fromJson 局部构建再原子提交(损坏载荷不污染活动游戏)；schemaVersion≠1 或 parse 失败 → warn+不应用。
  - Level.fromSave(w,h,level,tiles,data) 静态工厂 + 构造体加 skipGen 形参(早退不调 LevelGen、不自动 spawn AirWizard)；读档绕过生成。
  - Game：saveGame(守卫 !active + player.removed||health<=0 规避读档即死)/loadGame(try/catch)/hasSave；tick() 增 tickCount + 每1800 tick 自动存(仅活动游戏内)。
  - TitleMenu.vue buttons 改 computed 动态前置「继续游戏」+ emit('continue')；App.vue onContinue + beforeunload 接线。
  - 偏差披露：① Resource.getByName 早已存在(registry 已满足)未改；② Furniture.createByName 用子类自注册表(registerFurniture)规避 class-extends-value 循环依赖，六个家具类(铁砧/熔炉/箱子/灯笼/烤箱/工作台)均自注册，App.vue 启动即全注册；③ ts-port 无 ItemEntity 故跳过其持久化。
  - **主理人复核抓到的保真缺口(已修)**：Level.skipGen 早退分支只重算 dirtColor 未重算 monsterDensity(构造体对 level<0/sky 设4)；对切片无运行时影响(不自动刷怪)，但声明不实。已重新派 engineering-lead-2 补 `if (level<0||level===1) this.monsterDensity=4;` 并复跑三道门控确认仍绿。
  - **存档 MVP 最终收官(主理人独立复核签字)**：保真修正后亲自重跑三道门控，真实退出码全 0 —— TSC_EXIT=0 / VITEST_EXIT=0(12文件72用例全过，零回归) / VITE_EXIT=0(109 modules)。Read 确认 Level.ts:55 该行确实落地，与 L64/67 构造体分支规则对齐。TS 版存档 MVP 闭环正式交付：定时30s(1800 tick)+beforeunload 自动存、标题「继续游戏」动态出现、schemaVersion 容错、死亡/通关不自动存(规避读档即死)。
  - **vite.config 清理(用户指令"执行 vite.config 清理")**：删冗余 `vite.config.ts`(Vite 解析顺序 .mjs 优先于 .ts，.mjs 为生效份) + 删全部 `vite.config.*.timestamp-*.mjs` 临时文件(共 ~31 个，构建时自动再生)。沙箱删操作已放行(此前禁删，现已可删)。`emptyOutDir` 维持 `false`(沙箱 bulk-delete 守卫仍会拦 Vite 的 dist 清空扫描，重开 true 会让构建在本沙箱失败，等删除权限放开再启用)。清理后复核：VITE_EXIT=0(109 modules，且不再新生成 timestamp 文件，印证 .mjs 直载) / VITEST_EXIT=0(12文件72用例，Vitest 从 .mjs 正确读到 test.include)。遗留可选清理：`vitest-debug.log`(19KB 调试产物，非 vite.config 范围，未动)。
- **阶段1 全逻辑 port 收官**：S1~S6 六冲刺全部主理人独立复核绿（tsc 全程 exit0；vitest 64 测试 / 11 文件零回归；vite build 104 modules exit0）。游戏数据层(物品/背包/资源/合成/家具)、mobs+Boss、胜利连线、Vue 菜单树、粒子系统、五缺失 Tile 全部落地，AirWizard 端到端可达(击杀→WonMenu)。下一步可选：① 本地 ant+GWT 2.4 验证 GWT 侧汉化/大屏/存档改造(#4) ② 视用户需求补 ItemEntity/goldOre/gemOre/粒子文字与音效 ③ 清理 vite.config 冗余(双配置+timestamp 临时文件)。
- 用户要求评估 ts-port 代码质量（主要是 TS 版）。产出 `ts-port/CODE-QUALITY-REPORT.md`，综合评分 8/10。强项：架构分层清晰(engine 无 DOM 可单测 / game 逻辑 / ui Vue 桥接)、类型纪律严(strict 开 + 零 @ts-ignore + any 仅 9 处且生产代码 0 纯 any)、存档系统健壮(SaveStore localStorage/内存优雅降级 + SaveManager.fromJson 局部构建后原子提交防半更新 + Game.saveGame 防读档即死守卫)、测试有质量(save.test 8用例45断言覆盖 round-trip 字节级一致/损坏容错/自动存档节拍/实体多样性)。短板(均 P0/P1 工程化)：无 ESLint/Prettier/CI/覆盖率门槛、`tsconfig` 关了 noUnusedLocals/noUnusedParameters、8 处 console.* 调试残留(Game 3+SaveManager 5)、registry.ts 的 ironOre 颜色硬编码占位 0x333333 无 TODO(goldOre/gemOre 未 port)、反序列化无运行时 schema 校验(全靠 as number ?? 0 兜底)、Game.renderGui 裸 magic number、sky-scratch.test.ts 为 noop 占位。最高优先级动作：加 ESLint+Prettier+最小 CI、开 noUnusedLocals、清理 8 处 console 与 noop 测试——几乎零风险却能明显拉高可维护性分。
  - **vite 配置决策反转(用户新指令"1. 优先保留 vite.config.ts")**：上一步删 .ts 保留 .mjs 被推翻。因 Vite 解析顺序 `.mjs` 优先于 `.ts`，要让 `.ts` 生效必须删 `.mjs` → 重建 `vite.config.ts`、删 `vite.config.mjs`。构建验证 VITE_EXIT=0(109 modules)，且写回 `.ts` 后立即再生 `vite.config.ts.timestamp-*.mjs` 临时副本(证实 .ts 被 Vite 编译加载，符合用户意图)。此与 CODE-QUALITY-REPORT.md 第8条"清理冗余、Vite 取 .mjs"建议相反，但用户指令优先。
  - **参考 CODE-QUALITY-REPORT.md 做纯代码卫生(用户选"纯代码卫生"范围，不加 ESLint/Prettier/CI)**：调度 engineering-lead 落地四项并主理人独立复核绿。① 新增 `src/game/logger.ts` 统一日志汇聚点(零依赖)，替换 6 处生产 `console.warn`(Game 2 + SaveManager 4；报告称"8"含 2 处 doc 注释非调用，已正确识别)；SaveManager 导入路径须 `../logger`(非 `./logger`，否则 TS2307)。② 删 `sky-scratch.test.ts` noop(72→71 用例 / 12→11 文件)。③ `tsconfig` 开 `noUnusedLocals`/`noUnusedParameters`=true，修 5 文件死代码(TextParticle/Player/Level 未用 import、LevelGen 死链 `wval`/`wnoise1/2`、TextParticle 死字段 `msg`→`_msg`)——全为死代码清理，无运行时变化。④ `registry.ts` ironOre 硬编码 `0x333333` 补 TODO 标记。主理人独立复核：TSC_EXIT=0 / VITEST_EXIT=0(11文件71用例) / VITE_EXIT=0(110 modules，含 logger.ts)。报告 P0(ESLint/Prettier/CI)未做，留作下一步可选。
  - **vite.config.ts IDE 报错修复 + 玩法说明左对齐(主理人直办,agent 限流不可用)**：① IDE 报 vite.config.ts:3 `can only be default-imported using the esModuleInterop flag`。根因：根 tsconfig 第10行已有 `esModuleInterop:true` 但 `include:["src"]` 不覆盖 vite.config.ts，IDE 用推断项目(默认无 esModuleInterop)检查它。修：新建 `tsconfig.node.json`(esModuleInterop+allowSyntheticDefaultImports+noEmit，include 仅 vite.config.ts，不设 composite 避免 TS6378，不加根 references 避免强制 composite)；根 tsconfig 不动；vite.config.ts 内容一行未改(fallback 未触发——`/// <reference types="vitest" />` 的 augment 在当前 vitest 版本下生效，test 字段类型 OK)。② HelpMenu.vue `.help-list` 加 `text-align:left`(覆盖 .menu-frame 的 center)，5 条文案/加宽/min-width 460px 全保留。四道门控主理人亲自跑全绿：TSC_ROOT=0 / TSC_NODE=0(IDE 报错消除的代理验证) / VITEST=0(11文件71用例) / VITE=0(110 modules)。注：tsconfig.node.json 的 IDE 生效需用户在 VS Code 执行 "TypeScript: Restart TS Server"。
