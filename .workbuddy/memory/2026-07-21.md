# 2026-07-21 工作日志

- **整理游戏细节清单（用户指令"整理一份游戏细节清单"）**：产出 `ts-port/GAME-DETAILS-CHECKLIST.md`，全部数据直接核对磁盘真实源码（tile/registry、resource/registry、Crafting、InputHandler、Mob/Player/Zombie/Slime/AirWizard、Furniture、Game、EntityIO），非记忆臆测。覆盖 12 节：① 操作控制（7 键：方向/attack=C/ menu=X+Enter/ select=Z）② 5 维层级（Sky[4]→Surface[3]出生→Under1-3）③ 玩家状态（生命10/体力10/起始Workbench+PowerGlove）④ 方块清单 id0-22（含 iron/gold/gemOre 已注册）⑤ 资源 24 种 ⑥ 物品与工具（5 类型×5 级 木→石→铁→金→宝石）⑦ 合成 35 条（工作台16/铁砧15/熔炉3/烤箱1）⑧ 家具 6 种 ⑨ 怪物（Zombie lvl²×10 伤lvl+1 / Slime lvl²×5 伤lvl / AirWizard 2000HP）⑩ 存档（localStorage minicraft.save / 30s+beforeunload / t:1-12 含 ItemEntity）⑪ 已知偏差（GWT侧未编译/无音效/无ESLint）⑫ 闭环可达性。当前门控：tsc=0 / vitest 77 / vite 111 modules 全绿。

- **补存档说明（用户指出"游戏说明中没有存档相关的说明"）**：两处补。① `GAME-DETAILS-CHECKLIST.md` 第10节前插入玩家向「存档说明」段落（自动存约30s/关页也存、无手动按钮、标题「继续游戏」续玩、存什么、死亡/通关不覆盖、localStorage位置与清缓存丢档、损坏容错）。② 游戏内 `src/ui/menus/HelpMenu.vue` 的 `lines` 数组补两行玩家向存档说明（"进度每约30秒自动存档，关闭页面也会保存" / "标题界面出现「继续游戏」时可接续上次进度"）。三道门控复核：tsc=0 / vitest 77(12文件) / vite 111 modules 全绿。

- **修复菜单导航缺口（用户指出"没有从玩家说明进入游戏的入口，也没有从游戏进入玩家说明的方式"）**：双向打通。① 新增 `InputHandler` 的 `help` 键（KeyH/keyCode 72，id=7），`Game.tick()` 在 `isMenuOpen()` 守卫之后检测 `input.help.clicked` → `openHelp()`（仅游戏中可开，菜单打开期间被守卫拦截不会重触发）。② 引入 `state.returnMenu`：每个 opener 显式设定返回目标——从标题打开的 玩法说明/关于 关闭后回 `title`（标题「开始游戏」保持可达）；从游戏内打开的关闭后回 `none`（回到游戏）。修复了原先 `closeMenu()` 一律置 `none`、导致从标题看说明后落到空白死胡同的 bug。③ `closeMenu()` 仅当目标为 `none` 时吸收 menu 键（回标题无需吸收）。④ `HelpMenu.vue` 补一行"游戏中按 H 可随时打开本说明"并改提示为"X / Enter / Esc 返回（标题或游戏）"。⑤ `GAME-DETAILS-CHECKLIST.md` 第1节操作表补 KeyH 行 + 菜单返回逻辑说明。三道门控复核：tsc=0 / vitest 77(12文件) / vite 111 modules 全绿。

- **背包菜单加「玩法说明」按钮（用户："想要从背包菜单里点按钮进说明，不依赖 H 键记忆"）**：在 `InventoryMenu.vue` 加一个「玩法说明 (H)」按钮，emit 新事件 `help` → `App.vue` 在 `<InventoryMenu @help="openHelp">` 接住。这引出三层链 游戏→背包→说明→背包。原先 `state.returnMenu` 单字段方案在此会串味（关说明后回不到背包、或回背包自身死循环），故把状态层从单返回目标升级为**菜单栈 `gameState.menuStack`**：新增 `enterMenu(name)` 入栈当前菜单，`openInventory/Crafting/Container/Dead/Won/Help/About` 全部走 `enterMenu`，`closeMenu()` 改为弹栈 `menuStack.pop()` 回到精确上一层（仅目标为 `none` 时吸收 menu 键）。这样 背包→说明→背包 精确往返，且 `startSession()/showTitle()` 清空栈。`HelpMenu` 提示改为「返回（游戏 / 背包 / 标题）」，清单第1节补「背包菜单点按钮进入」说明。三道门控复核：tsc=0 / vitest 77(12文件) / vite 111 modules 全绿。

- **修复"采矿/耕地/战斗/上下楼/合成全断" + 补种植/作物链路（用户三项指令）**：经核对磁盘真实源码，发现 `AUDIT-TS-VS-JAVA.md`（2026-07-20）已严重过时——`Player.attack` 四段式管线、`Mob.hurt`、`Ore/Rock/HardRock` 的 `interact`+`hurt`+掉落、`Grass` 耕地、`Tree` 掉橡果、`Sapling` 生长、`Wheat` 收割、`Game.changeLevel`（上下楼）、`Crafting` 35 配方、`EntityIO` 的 `t:12` 地面掉落物(ItemEntity)持久化 其实**早已实现并三道门控绿**。真正的唯一断点是 **`InventoryMenu.vue` 只浏览不装备**：选中物品从不写入 `player.activeItem`，导致玩家永远拿不到工具/种子/工作台，从而使采矿(需镐)、耕地(需锄)、种植(需手持橡果/种子)、放工作台→合成 整条链"看似全断"。修复：① `state.ts` 新增 `equipItem(item)`（写 `activeGame.player.activeItem`）；② `InventoryMenu.vue` 新增 emit `select(item)`，C/Enter/点行 = 装备并关闭，X/Esc = 仅关闭(保留当前手持)；③ `App.vue` 接 `@select="(item)=>{ equipItem(item); game.input.attack.absorb(); closeMenu(); }"`（吸收攻击键，避免装备瞬间误触一次攻击/放家具）。种植链路因此贯通：橡果(PlantableResource)→grass→treeSapling(SaplingTile)→age>100 长成树；仙人掌花→sand→cactusSapling→cactus；种子→farmland→wheat→收割掉小麦+种子。ItemEntity 存档持久化本就已在（EntityIO t:12 + SaveManager 重载时 `lvl.add(e)`），无需改动，仅复核。新增测试 `menus.test.ts` 验证 InventoryMenu 点击 emit 正确的 `select` 物品。三道门控复核：tsc=0 / vitest **78**(12文件) / vite 111 modules 全绿。

- **Wheat 中段生长视觉（用户："补一个小的 Wheat 中段生长视觉（目前种子直接变满熟小麦，可选更拟真）"）**：根因非 bug——种下时 `setTile(wheat,0)` 龄从 0 起，而是 **长太快(原 tick ~50%概率+1，0→50 仅≈1.6s) + 早期配色平(age<30 全用土褐，仅换 sprite)** 让人觉得"种子直接变满熟"。改造 `ts-port/src/game/level/tile/Wheat.ts`：① `render` 连续绿→金渐变——`icon=min(3,floor(age/13))` 取 4 帧(0 嫩芽→3 成株)，body 色随帧 `50+icon*100`(绿50→黄绿350)，age≥50 用 GWT 金黄 `Color.get(0,0,150,240)`；轮廓保留土褐(shade a/b)维持麦秆形状。② `tick` 放缓：仅 `Math.random()<0.8` 时跳过→约 20% 概率+1，≈4s 熟(60fps)，肉眼可见分段生长。另把过时"掉落是 stub"注释改正。新增测试 `sprint6.test.ts`：种下龄=0、40 tick 后仍 <50(非瞬间满熟)、2000 tick 后封顶 50。三道门控复核：tsc=0 / vitest **79**(12文件) / vite 111 modules 全绿。

- **Java↔TS 移植差距复查（用户："再次和java版本对比下，看看有哪些遗漏"）**：旧 `AUDIT-TS-VS-JAVA.md`(2026-07-20) 已严重过时（Player.attack stub/tiles stub/不刷怪/gold-gem未注册/ItemEntity不进档 等核心论断全不成立）。派 Explore agent 通读两树做行为 diff + 主理人亲自核实 P1/P2 关键点，产出 `ts-port/GAP-VS-JAVA-2026-07-21.md`。**真实缺口（无 P0）**：① P1 胜利/死亡演出延时缺失——`Game.tick`(Game.ts:266-270) 在 `hasWon`/`health<=0` 立即 `openWon/openDead`，`won()` 设的 `wonTimer=180` 从不倒数（Java 有 3s/1s 演出）；② P1 HUD 缺体力条+手持物图标——`renderGui`(Game.ts:311-325) 只画红心，无 Java(Game.java:510-541) 的体力条与 `activeItem.renderInventory`；③ P2 音频系统完全缺失（无 sound 模块）；④ P2 位图字体文本未实现——`TextParticle.render`(TextParticle.ts:57-61) 只画彩色方块、`_msg` 被忽略（Java 用 Font.draw 显数字）；⑤ P2 维度切换过场菜单缺失（`changeLevel` 瞬时换层）；⑥ P2 玩家渲染缺挥动/手持家具；⑦ P2 失焦提示缺失。已忠实移植：地下光照、玩家(swim/体力/attack四段/无敌帧/食物)、怪AI、全部方块、物品、菜单(含标题「继续游戏」/Container/Crafting/Inventory/Won/Dead/About/Help)、ItemEntity 存档、gold/gem 注册。建议优先修：P1#1→P1#2→P2#4(字体)→P2#3(音频)→P2#5(过场)。

- **按差距清单逐项修复全部 7 项（用户："时间有的是，都修复了"）**：主理人编排，建团队 `gapfix`，三路工程师并行（文件归属不重叠，音频串行避免碰 Player/Game）。① eng-flow 修 P1#1(胜负演出延时:Game.tick 加 deadTime>60 开 DeadMenu + wonTimer≤0 开 WonMenu)、P1#2(HUD 加 10 格体力条含闪烁 + activeItem.renderInventory 图标)、P2#5(changeLevel 改延迟 pendingLevelChange + 新 TransitionMenu.vue 过场~1s 后 completeLevelChange)、P2#7(App.vue 失焦显「点击聚焦！」)；动 Game.ts/state.ts/App.vue/TransitionMenu.vue。② eng-render 修 P2#4(新 engine/Font.ts 移植位图字体——核实 SpriteSheet 字形坐标+内置5x7兜底、Screen.draw/renderGlyph、TextParticle 改调 Font.draw 显真实数字)、P2#6(Player.render 加四向挥动手势+手持家具精灵，用既有 attackTime/attackDir/attackItem)；动 Screen.ts/Font.ts/TextParticle.ts/Player.ts。③ audio-lead 修 P2#3(新 game/audio/Sound.ts，Web Audio 实时合成、零二进制资源，绕过自动播放策略；接 monsterHurt/playerHurt/playerDeath/bossdeath；pickup/craft 已定义未自动接线)。**最终合并门控全绿：tsc=0 / 89 测试(14文件) / vite 116 modules**。两份新测试：engine/__tests__/font.test.ts(7)、game/__tests__/player-render.test.ts(3)。已知小偏离：P1#2 因 Player.staminaRechargeDelay 为 private 用结构性 cast 读取(功能等价)、P2#3 pickup/craft 未自动接线。差距报告 `ts-port/GAP-VS-JAVA-2026-07-21.md` 已补「修复结果」章节。均未 git commit。

- **[git 提交错误分析]** 用户在 IDE 提交时遇 `invalid path 'nul' unable to add 'nul' to index` 阻断 + `minicraftHtml5.iws` 的 `LF will be replaced by CRLF` warning。根因：① 仓库根目录有个 0 字节文件 `nul`（mtime 2026-07-20 12:04），Windows 把 `nul`(NUL) 当保留设备名(/dev/null)，Git on Windows 无法将其加入索引，每次 `git add .`/IDE 提交尝试 add 它即失败阻塞；它未被追踪（invalid path 致 Git 无法枚举）也未被 .gitignore 忽略。② `.iws` 是 IntelliJ 个人工作区文件（本不应纳入版本控制），`core.autocrlf` 在 add 时 LF→CRLF 触发 warning，且它**已被追踪**故加 .gitignore 规则前仍被 add。修复（已落地）：`.gitignore` 追加 `nul` + `*.iws`/`*.ipr`/`.idea/`（让 `git add .` 跳过 nul、忽略 IDE 文件）；`git rm --cached minicraftHtml5.iws minicraftHtml5.ipr`（取消追踪、物理文件保留）→ 消除 CRLF warning。验证：`git add -n .` 不再报 invalid path 'nul'、`grep -iE 'nul|\.iws$|\.ipr$'` 无匹配 → 提交阻塞解除。⚠️ 额外重大发现：`git add -n .` 列出大量 `A ts-port/node_modules/.bin/...` 等且状态 `A`（已 staged），说明 **ts-port/node_modules 已被 Git 追踪**（`.gitignore` 的 `ts-port/node_modules` 对已被追踪文件无效）；若直接提交会把整个依赖树封进 commit。建议（未执行，待用户拍板）：`git rm -r --cached ts-port/node_modules`（物理保留仅取消追踪）+ 现有 ignore 规则已防未来重入。nul 物理文件因 Windows 保留名常规 rm/回收站均失败（safe-delete 报“指定的设备名无效”/“Win32 设备路径”），已被 Git 忽略故不再阻塞提交；如需从磁盘彻底清，用 PowerShell `.NET` + `\\?\` 长路径前缀：`[System.IO.File]::Delete('\\?\D:\MyDevelop\JSDevelop\Minicraft-HTML5\nul')`（勿用 `\\.\` 前缀）。分析文档产出 `GIT-COMMIT-ISSUE-ANALYSIS.md`（根目录）。

- **修复二次复核发现的 2 个 P1（用户选"修 P1-A + P1-B"）**：主理人派 engineering-lead 落地，独立复核三道门控（tsc=0 / 96 测试(16文件) / vite 116 modules 全绿）确认无回归。① P1-A：`Mob.ts` 新增 `hurtTile(_tile,_x,_y,dmg){ this.doHurt(dmg, this.dir^1); }`(Mob.ts:95-97)，令 `Ore.bumpedInto`(Ore.ts:41)/`Cactus.bumpedInto`(Cactus.ts:33) 调 `hurtTile` 现真实掉血（矿石3/仙人掌1），对齐 Java `Mob.hurt(Tile,…)`。② P1-B：`Dirt.ts` 补 imports + `interact()`(Dirt.ts:27-45)——铲子挖洞(`Tile.hole`)+掉 `Resource.dirt` 的 `ItemEntity`、锄头锄成 `Tile.farmland`；`Sound.monsterHurt` 故意省略（与 `Grass.interact` 一致，P2-3 音频缺口不在本次范围）。新增 `entity/__tests__/mob.test.ts`(4) + `level/tile/__tests__/dirt.test.ts`(3)。均未 git commit。至此 P1 全清零，剩 P2×10（含 P2-1 小麦减速为用户有意保留）。GAP 报告 `GAP-VS-JAVA-2026-07-21.md` 已补「二次复核修复结果」章节。

- **三次 Java↔TS 复核（用户再次"再次和java版本对比下"）**：P1-A(`Mob.hurtTile`,Mob.ts:95-97)/P1-B(`Dirt.interact`,Dirt.ts:27-45) 经 Explore+主理人独立核实均正确修复，P1=0；7 早期修复+2 收净完好；原 P2-1~10 仍有效（多数为有意选择/无害/打磨）。**新发现 P2-11：`Ore.ts:29` 误用四参 `Color.get`（`Color.get(d,d,d,d)` 把 get1 加到全部 4 字节）污染矿石配色，应改为 `Color.get1(level.dirtColor)`；且构造器掩码 `0xffffff00`(Ore.ts:25) 与 Java `0xffff00`(OreTile.java:25) 不同（Java 清最低 2 字节）——纯视觉 bug，非崩溃。verdict：P0=0 / P1=0 / P2=11。报告已补「三次复核」章节。全部改动尚未 git commit。

- **二次 Java↔TS 复核（用户再次"再次和java版本对比下，看看有哪些遗漏"）**：清理团队 `cleanup` 收净 2 偏离（`Player.staminaRechargeDelay` 改 public 去 cast、`Recipe.craft`/`ItemEntity` 拾取处接 `Sound.play('craft'/'pickup')`）后，主理人跑三道门控（tsc=0 / 89 测试 / 116 modules 全绿）确认无回归，再派 Explore agent 通读两树做行为 diff + 主理人 grep 独立核实。结论：**8 项已修复全部确认正确 ✅；P0=0、P1=2、P2=10**。新 P1：① `Mob` 未重写 `hurtTile`（`Entity.ts:44` 空实现），致 `Ore.bumpedInto`(Ore.ts:41)/`Cactus.bumpedInto`(Cactus.ts:33) 调 `hurtTile` 成空操作——碰矿(应3伤)/仙人掌(应1伤)不掉血；② `Dirt` 瓦片缺 `interact()`（Dirt.ts 仅 class 声明），无法挖洞/直接锄地。新 P2：小麦生长速率仅 Java 40%(用户有意偏离)、小麦配色斜坡不同、挖/锄瓦片缺 `Sound.monsterHurt`、`SmashParticle` 缺该音、`TitleMenu` 缺 `Sound.test`、失焦提示不闪烁、家具存档缺 pushTime/pushDir、Mob 存档缺 xr/yr、键位少于 Java、`ToolItem` 不画名。报告 `GAP-VS-JAVA-2026-07-21.md` 已补「二次复核」章节。均未 git commit。

- **四次 Java↔TS 复核 + P2 全清（用户选"P2-11 + 音频/打磨项"）**：主理人派 engineering-lead 逐项读 Java 原版做忠实最小改动（触 max-turns(45) 中断，但落盘改动经主理人 grep/Read 逐文件核实完整、编译通过、测试不破）。修复：P2-11(Ore.ts:27,34 掩码0xffff00+`Color.get1(dirtColor)`) / P2-3(Grass.ts:69,80 + Dirt.ts:35,42 `interact` 成功补 `Sound.play('monsterHurt')`) / P2-4(SmashParticle.ts:19 构造补该音) / P2-5(TitleMenu.vue:43 补 `Sound.play('test')`，Sound.ts 新增 `test` 音) / P2-6(App.vue:246-261 `.focus-nagger` 加 `focusBlink 0.66s step-end` 动画) / P2-7(EntityIO.ts 家具分支持久化 pushTime/pushDir) / P2-8(EntityIO.ts writeMobCommon 持久化 xr/yr) / P2-9(InputHandler.ts:83-90,109-114 补 Tab/Alt=menu、Space/Ctrl/0/-=attack，保留 KeyC/KeyX/Enter/Z/H) / P2-10(ToolItem.ts:60 `renderInventory` 补 `Font.draw(getName())`)。三道门控全绿：tsc=0 / 96 测试(16文件) / vite 116 modules。verdict：P0=0 / P1=0 / P2=0（全部清零，P2-1/2 小麦有意保留）。报告已补「四次复核」章节。**遗留：`ts-port/node_modules` 仍被误 staged（数千文件），提交前须 `git reset ts-port/node_modules` 移除，否则 commit 带进依赖。** 全部未 git commit。

- **机制问答（用户运行中提问"怎么吃背包里的食物 / 力量手套攻击力变0"）**：
  ① 吃食物：TS 版**当前无法吃**。Java `item/resource/FoodResource.java` 存在（`interactOn` 判定 `health<maxHealth && payStamina` 后 `heal`，手持食物对地面按使用键触发）；但 TS 版**无 `FoodResource` 子类**（TS 路径 `src/game/item/resource/`，只有 `Resource.ts`），且 `Resource.interactOn` 恒返回 false（`Resource.ts:54`），`bread`/`apple` 注册为普通 `Resource` → 拿食物按键不回血。属未接通功能（差距审计"吃食物不可达"）。待用户决定是否 port `FoodResource` + 接线（新功能，非修 bug）。
  ② 力量手套"攻击力0"：**非 bug，原版设定**。`PowerGloveItem extends Item` 未重写 `canAttack()`（默认 false，`Item.ts:45`）；`Player.attack()` 第217行 `if(activeItem==null || activeItem.canAttack())` 跳过攻击分支 → 拿它**完全打不出攻击**（连空手1~3基础伤害都没有）。力量手套本职是搬家具工具，非武器。Minicraft 无穿戴栏，物品均手持（选中即"戴"）。要战斗用剑/斧/空手。

- **【更正·吃食物机制问答的误判】**：上方「机制问答①」结论"TS 版当前无法吃食物 / 无 FoodResource 子类"**有误**，须更正。根因是我当时 Glob `ts-port/src/game/item/FoodResource*.ts`（漏了 `resource/` 层）报 "No files found"，但真实文件在 `ts-port/src/game/item/resource/FoodResource.ts`，且 `registry.ts:18` 已 `import { FoodResource }` 将其用于 `bread`/`apple`。因此吃食物链路**早已完整实现**：`Player.attack()`→`item.interactOn(tile,level,xt,yt,player,attackDir)`→`FoodResource.interactOn`（`health<maxHealth && payStamina(staminaCost)` 时 `heal(heal)`）。本会话真正补的是**可感知性缺口**：① `Player.feedbackFood()`（Player.ts:259-275）按结果弹出绿"+N"/黄"吃饱了"/黄"体力不足"文字粒子；② `Sound.ts` 新增 `eat` 合成音（2 段正弦上滑）；③ `HelpMenu.vue` 补一行"手持食物对地面按攻击键(C/空格)可回血"；④ 新增 `game/__tests__/eating.test.ts`(6) 覆盖回血/消耗/满血无效/体力不足/文字+音效。三道门控复核：tsc=0 / vitest **102**(17文件，含 +6 eating) / vite 116 modules 全绿。**结论：吃食物原就能用，只是没有反馈，现已补齐。**

- **标题菜单显式选项（用户："在游戏启动时添加选项：1. 重新开始 2. 读取存档"）**：改 `ts-port/src/ui/menus/TitleMenu.vue`，将原本动态 `buttons`（有存档前置「继续游戏」、否则仅「开始游戏」）重构为结构化 `options: TitleOption[]{label,enabled,kind}`。四项恒显：**重新开始**(kind=start,emit('start'),永远可用) / **读取存档**(kind=continue,emit('continue'),仅 `getActiveGame()?.hasSave()` 时可用) / 玩法说明(help) / 关于(about)。**读取存档**无存档时置灰禁用：键盘导航 `move()` 跳过 disabled 项、鼠标 hover/click 对禁用项 inert、`onItemClick`/`confirm` 对禁用项 no-op，杜绝选到空槽。挂载时 `setSelectedIndex(0)` 默认高亮「重新开始」。`App.vue` 已绑定 `@start/@continue/@help/@about`，**无需改动**。三道门控全绿：tsc=0 / vitest 102(17文件) / vite 116 modules。⚠️ 提示（未实现，待用户拍板）：「重新开始」会覆盖现有存档（原版行为：新游戏后自动存档覆盖），若需"覆盖前二次确认"可再加一道守卫。均未 git commit。

- **标题菜单「覆盖前二次确认」守卫（用户："加一道覆盖前二次确认守卫"）**：在上一轮的 TitleMenu.vue 基础上补确认弹窗。选「重新开始」且 `hasSave()` 为真时，不再直接 `emit('start')`，而是 `confirming=true` + `confirmIndex=1`（默认聚焦「取消」防误覆盖），DOM 切换为确认面板（提示"开始新游戏将覆盖当前存档，确定吗？"+「确定」/「取消」）。确认面板内：方向键/A-D/W-S 切换 确定↔取消、Enter/X 触发 `resolveConfirm()`、Esc `cancelConfirm()`；鼠标 hover 设 index、click `onConfirmClick` 直接结算。仅当选「确定」(index 0) 才 `Sound.play('test')` + `emit('start')`；选「取消」返回主菜单。无存档时「重新开始」仍直开新游戏（无覆盖风险，无需确认）；「读取存档」只读不写，无确认。三道门控全绿：tsc=0 / vitest 102(17文件) / vite 116 modules。均未 git commit。
